using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShieldController : MonoBehaviour
{
    public bool moveShield;
    public Transform lerpTo;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    //void Update()
    //{
    //    //if (moveShield)
    //    //{
    //    //    transform.position = Vector3.Lerp(transform.position, lerpTo.position, Time.deltaTime);
    //    //    //transform.rotation = Quaternion.Euler(lerpTo.rotation.x, lerpTo.rotation.y, lerpTo.rotation.z);
    //    //   // transform.Rotate(transform.rotation.x, transform.rotation.y, transform.rotation.z, Space.Self);
    //    //}
    //}
}
